version: 2
project_name: miro-cli
changelog:
  disable: true
builds:
  - id: miro-cli
    main: ./cmd/miro-cli
    binary: miro-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X miro-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: miro-cli
    repository:
      owner: olga-safonova
      name: homebrew-tap
    homepage: "https://github.com/olga-safonova/miro-cli"
    description: "<img src=\"https://content.pstmn.io/47449ea6-0ef7-4af2-bac1-e58a70e61c58/aW1hZ2UucG5n\" width=\"1685\" height=\"593\"> ###..."
    install: |
      bin.install "miro-cli"
